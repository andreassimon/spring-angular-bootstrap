apply plugin: 'groovy'

repositories {
    maven { url 'https://plugins.gradle.org/m2/' }
    maven { url 'http://repo.spring.io/plugins-release' }
    jcenter()
}

dependencies {
    compile "gradle.plugin.com.github.jk1:gradle-license-report:0.3.5"
}
